import styled, { css } from 'styled-components';
import { sif } from '@common/styled';

const StyledApp = styled.div`
  color: rgb(51, 72, 101);
  overflow-x: hidden;
  video,
  img {
    display: block;
    max-width: 100%;
  }
`;

export { StyledApp };
