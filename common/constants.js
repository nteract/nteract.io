const unit = 12;
const gutter = unit * 2;
const globalWidth = 1250;
const headerHeight = 100;

const colors = {
  darknavy: '#334865',
};

const fonts = {
  headings: `"Nunito", sans-serif`,
  body: `-apple-system, system-ui, BlinkMacSystemFont, "Segoe UI",
  Roboto`,
};

const spacing = {
  unit,
  gutter,
  globalWidth,
  headerHeight,
};

export { spacing, colors, fonts };
